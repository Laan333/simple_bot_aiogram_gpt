# Telegram AI Chatbot

Telegram бот с интеграцией ChatGPT API, использующий aiogram 3 и PostgreSQL/SQLite для хранения контекста диалогов.

## 🚀 Возможности

- ✅ Обработка команд `/start` и `/help`
- ✅ Интеграция с ChatGPT API (актуальные модели семейства GPT‑4o) для генерации ответов в Markdown
- ✅ Автоопределение языка пользователя (ответ на том же языке)
- ✅ Сохранение истории диалогов в PostgreSQL или SQLite (переключается через `.env`)
- ✅ Использование контекста последних 5 сообщений для более качественных ответов
- ✅ Сброс контекста через команду `/start` или кнопку "Новый запрос"
- ✅ Современная архитектура с использованием aiogram 3 и лучших практик

## 📋 Требования

- Python 3.10+
- PostgreSQL 12+ **или** SQLite 3 (встроен в Python)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (получить на [platform.openai.com](https://platform.openai.com/api-keys))

## 🛠️ Установка

1. **Клонируйте репозиторий или скачайте проект**

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Выберите и настройте базу данных:**

   **Вариант 1: SQLite (проще, по умолчанию)**
   - Никаких дополнительных действий не требуется: файл БД (например, `db.sqlite3`) будет создан автоматически.

   **Вариант 2: PostgreSQL**
   - Создайте базу данных:
     ```sql
     CREATE DATABASE telegram_bot;
     ```
   - Или используйте существующую базу данных

6. **Настройте переменные окружения:**
   - Скопируйте `.env.example` в `.env`:
     ```bash
     copy .env.example .env
     ```
   - Откройте `.env` и заполните необходимые значения (минимальный пример):
     ```env
     BOT_TOKEN=your_bot_token_here
     OPENAI_API_KEY=your_openai_api_key_here

     # Тип БД: postgresql или sqlite
     DB_TYPE=sqlite

     # Настройки PostgreSQL (если используете PostgreSQL)
     DB_HOST=localhost
     DB_PORT=5432
     DB_NAME=telegram_bot
     DB_USER=postgres
     DB_PASSWORD=postgres

     # Настройки SQLite (если используете SQLite)
     SQLITE_PATH=db.sqlite3

     # Модель OpenAI (по умолчанию: gpt-4o-mini)
     OPENAI_MODEL=gpt-4o-mini

     # Максимальное количество сообщений в контексте (по умолчанию: 5)
     MAX_CONTEXT_MESSAGES=5
     ```

## 🏃 Запуск

Запустите бота:
```bash
python main.py
```

Бот автоматически создаст необходимые таблицы в базе данных при первом запуске.

## 📁 Структура проекта

```
telegram-ai-chatbot/
├── main.py                 # Точка входа приложения
├── config.py               # Конфигурация приложения
├── requirements.txt        # Зависимости проекта
├── .env.example            # Пример файла с переменными окружения
├── README.md               # Документация
├── database/               # Модуль работы с БД
│   ├── __init__.py
│   ├── models.py           # Модели SQLAlchemy
│   ├── connection.py       # Подключение к БД
│   └── repository.py       # Репозиторий для работы с данными
├── handlers/               # Обработчики сообщений и команд
│   ├── __init__.py
│   ├── commands.py         # Обработчики команд (/start, /help)
│   └── messages.py         # Обработчики текстовых сообщений
├── keyboards/              # Клавиатуры бота
│   ├── __init__.py
│   └── inline.py           # Inline клавиатуры
└── services/               # Бизнес-логика
    ├── __init__.py
    └── chatgpt.py          # Сервис для работы с ChatGPT API
```

## 🎯 Использование

1. Найдите вашего бота в Telegram по имени, которое вы указали при создании у [@BotFather](https://t.me/BotFather)

2. Отправьте команду `/start` для начала работы

3. Отправьте любое текстовое сообщение — бот ответит с использованием ChatGPT в формате Markdown

4. Бот запоминает контекст последних 5 сообщений для более качественных ответов

5. Используйте команду `/start` или кнопку "🔄 Новый запрос" для сброса истории диалога

## ⚙️ Конфигурация

Все настройки находятся в файле `.env`:

- `BOT_TOKEN` - токен Telegram бота
- `OPENAI_API_KEY` - API ключ OpenAI
- `OPENAI_MODEL` - модель ChatGPT (по умолчанию: `gpt-4o-mini`; можно указать любую поддерживаемую модель GPT‑4o/4.1)
- `DB_TYPE` - тип базы данных: `postgresql` или `sqlite` (по умолчанию: `postgresql`, если не указано иное)
- `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` - настройки PostgreSQL (используются, если `DB_TYPE=postgresql`)
- `SQLITE_PATH` - путь к файлу SQLite (используется, если `DB_TYPE=sqlite`)
- `MAX_CONTEXT_MESSAGES` - максимальное количество сообщений в контексте (по умолчанию: 5)

## 🗄️ База данных

Бот автоматически создает таблицу `messages` со следующей структурой:

- `id` - уникальный идентификатор сообщения
- `user_id` - ID пользователя Telegram
- `message_text` - текст сообщения пользователя
- `response_text` - текст ответа бота
- `created_at` - время создания записи

## 🔧 Технологии

- **aiogram 3** - современный фреймворк для создания Telegram ботов
- **PostgreSQL / SQLite** - системы хранения данных
- **SQLAlchemy** - ORM для работы с БД
- **asyncpg** и **aiosqlite** - асинхронные драйверы для PostgreSQL и SQLite
- **OpenAI API** - API для работы с моделями семейства GPT‑4o
- **python-dotenv** - загрузка переменных окружения

## 📝 Примечания

- Бот использует контекст последних 5 сообщений (не включая текущее)
- При первом сообщении контекст пуст
- История диалога хранится в выбранной БД (PostgreSQL или SQLite) и сохраняется между перезапусками бота
- Для сброса истории используйте `/start` или кнопку "Новый запрос"

## 🐛 Решение проблем

### Ошибка подключения к базе данных
- Убедитесь, что PostgreSQL запущен (если используете PostgreSQL)
- Проверьте правильность настроек в `.env`
- Убедитесь, что база данных создана (для PostgreSQL) или что путь к SQLite-файлу корректен

### Ошибка при обращении к ChatGPT API
- Проверьте правильность `OPENAI_API_KEY`
- Убедитесь, что у вас есть доступ к API и достаточный баланс

### Бот не отвечает
- Проверьте правильность `BOT_TOKEN`
- Убедитесь, что бот запущен и нет ошибок в логах

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Проект создан для демонстрации работы с aiogram 3, PostgreSQL/SQLite и ChatGPT API.
